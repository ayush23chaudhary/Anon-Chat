package com.anonchat.security.provider;

import org.springframework.stereotype.Component;

/**
 * JWT Token Provider.
 * 
 * Handles JWT token generation, validation, and claims extraction.
 * Security: Uses HS512 algorithm with 256-bit keys.
 */
@Component
public class JwtTokenProvider {
    // JWT provider logic will be implemented here
}
