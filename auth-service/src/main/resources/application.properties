# Auth Service Configuration
spring.application.name=anonchat-auth-service

# Database Configuration (In-Memory H2 for development)
spring.datasource.url=jdbc:h2:mem:anonchat
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA/Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# H2 Console (for development only)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Logging Configuration
logging.level.root=INFO
logging.level.com.anonchat=DEBUG

# JWT Configuration
app.jwt.secret=anonchat-secret-key-minimum-32-characters-for-production-use-only-change-this
app.jwt.expiryMs=3600000
